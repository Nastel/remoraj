package com.jkoolcloud.remora.advices;

import org.junit.Test;

//Enable power mockito if any of classes failing to mock
//@RunWith(PowerMockRunner.class)
//@PrepareForTest({WebApp.class})
//@SuppressStaticInitializationFor({""})
public class SocketAdviceTest {

	@Test
	public void testSocketAdviceInterceptor() throws NoSuchMethodException {
		// PowerMockito.mockStatic(<<classToIntercept>>.class);
		// WebApp webApp=mock(<<classToIntercept>>.class);

		// EntryDefinition handleRequestEntry = new EntryDefinition(SocketAdvice.class);

		// Method method=Whitebox.getMethod(Object.class,"<<interceptingMethod>>");

		// test before method
		// SocketAdviceAdvice.before();

		// test after method
		// SocketAdviceAdvice.after();
	}
}